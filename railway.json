{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "sh -c 'if [ -d backend ]; then cd backend; fi; npm ci'"
  },
  "deploy": {
    "startCommand": "sh -c 'if [ -d backend ]; then cd backend; fi; npm start'",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
